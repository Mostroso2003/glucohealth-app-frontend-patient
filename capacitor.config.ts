import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'glucohealth-app-frontend-patient',
  webDir: 'dist'
};

export default config;
